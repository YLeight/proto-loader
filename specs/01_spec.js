describe('First spec description:', function () {
  it('first case in spec', function () {
    expect(true).toBe(true);
  });
  it('second case in spec', function () {
    expect(true).toBe(false);
  });
});

